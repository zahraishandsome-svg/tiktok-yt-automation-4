# TikTok to YouTube Channel Pairs
#
# ch6 (@mogumogu_lily) PERMANENTLY RETIRED 2026-07-26 (poor performance).
# This repo is ARCHIVED (read-only) and NOT reused. Per standard onboarding policy,
# every new channel gets its OWN brand-new repo + OWN GCP project under its OWN Gmail
# (keeps identities de-correlated). Cron jobs deleted; GCP project to be deleted.

channels:
  - id: channel_6
    tiktok_username: ""
    youtube_channel_name: ""
    google_credentials_file: "credentials/channel_6_client_secret.json"
    oauth_token_file: "tokens/channel_6_token.json"
    videos_per_day: 2
    description_footer: ""
    default_tags:
      - viral
      - trending
    youtube_category_id: "22"
    enabled: false
    max_retry_days: 7
    shorts_max_seconds: 180
    upload_mode: short_only
    slot_publish_times_utc:
      1: "09:00"   # PUBLIC at 2 PM PKT (UTC+5)
      2: "10:00"   # PUBLIC at 3 PM PKT (UTC+5)
